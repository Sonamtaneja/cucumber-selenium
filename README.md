# cucumber-selenium

Install POM:
mvn archetype:generate -DgroupId=com.practice -DartifactId=cucumber-practice -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false

Go to project folder
cd cucumber-practice

Run the test:
mvn clean test -DsuiteXmlFile=testng.xml